<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fundo branco para manter contraste no dark mode -->
    <solid android:color="#FFFFFF" />

    <!-- Borda cinza clara -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0" />

    <!-- Cantos arredondados -->
    <corners android:radius="10dp" />

    <!-- Espaçamento interno -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
